# B09 — Line Plot with Precomputed Uncertainty

This benchmark binds two explicit series to a fixed CSV, preserves one missing
observation as a gap, and renders precomputed 95% confidence bounds without
interpolation or statistical recomputation.
